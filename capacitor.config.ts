import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eliasadams.playground',
  appName: 'Playground',
  webDir: 'dist/src/browser',
};

export default config;
